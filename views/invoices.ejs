<%- include ('partials/top') %>

    <div class="row space pb-0 mb-4 px-2 border-bottom">
      <div class="col-12 d-flex justify-content-between align-items-center underline pb-2">
        <h3 class="title">Invoices</h3>
        <div>
          <a href="/invoices/new" class="btn btn-success">Create Invoice</a>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-12">
        <form id="search-form" method="GET" action="/invoices?" class="form-inline bordered">
          <div class="form-group mb-4 text-white">
            <input required type="text" name="search" class="form-control" placeholder="invoice name or date ...">
            <button type="submit" class="btn btn-info">Search</button>
          </div>
        </form>
        <h4 class="font-weight-light text-center text-white bg-primary p-2 m-0 table-top">List of Invoices</h4>
        <table class="table table-bordered table-striped">
          <thead class="font-weight-normal">
            <tr>
              <td>Invoice Id</td>
              <td>Customer Name</td>
              <td>Description</td>
              <td>Total Amount</td>
              <td>Amount Paid</td>
              <td>Transaction Date</td>
              <td>Actions</td>
            </tr>
          </thead>
          <tbody>
            <% invoices.forEach((m) => { %>
              <tr>
                <td><%= m._id %></td>
                <td><%= m.customer_name %></td>
                <td><%= m.description %></td>
                <td><%= m.total_amount %></td>
                <td><%= m.amount_paid %></td>
                <td><%= m.transaction_date.toLocaleDateString() %></td>
                <td>
                  <a href='<%= "/invoices/remove/" + m._id %>' class="btn btn-sm btn-danger">Remove</a>
                </td>
              </tr>
            <% }) %>          
          </tbody>
        </table>
      </div>
    </div>
  </main>
</div>
</div> 

<%- include ('partials/bottom') %>